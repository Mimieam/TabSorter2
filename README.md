# TabSorter2
Google Chrome Tab Management Extension - Merge, Sort, split and more :)

- Merge
- sort
- split
- Save - save current window to a text file  ( the links are shorten using google URL Shortener and the file content is sorted by title )
- Deduplicate
